import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary   = Color(0xFF1B3D2F);
  static const secondary = Color(0xFF2E614B);
  static const tertiary  = Color(0xFF512D2A);
  static const neutral   = Color(0xFF767775);

  // Surfaces (light theme)
  static const background    = Color(0xFFFFFFFF);
  static const surface       = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF5F5F5);

  // Text
  static const onPrimary     = Color(0xFFFFFFFF);
  static const onSecondary   = Color(0xFFFFFFFF);
  static const onBackground  = Color(0xFF1A1A1A);
  static const textMuted     = Color(0xFF767775);

  // Semantic
  static const error   = Color(0xFF512D2A);
  static const success = Color(0xFF2E614B);
}
